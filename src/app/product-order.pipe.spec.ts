import { ProductOrderPipe } from './product-order.pipe';

describe('ProductOrderPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductOrderPipe();
    expect(pipe).toBeTruthy();
  });
});
